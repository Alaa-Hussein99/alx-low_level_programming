#include "main.h"
/**
 * add - adds two numbers
 * Description: takes a and b and add them together and return the result
 * @a: first number to add
 * @b: second number to add
 * Return: the addition
*/
int add(int a, int b)
{
	return (a + b);
}
